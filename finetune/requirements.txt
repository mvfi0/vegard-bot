# Fine-tuning dependencies — install separately from the bot
# pip install -r finetune/requirements.txt

torch>=2.3.0
transformers>=4.45.0
peft>=0.12.0
trl>=0.11.0
bitsandbytes>=0.43.0   # 4-bit quantization (QLoRA)
accelerate>=0.34.0
datasets>=3.0.0
huggingface_hub>=0.24.0
